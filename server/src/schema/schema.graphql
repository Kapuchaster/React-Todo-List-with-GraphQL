type Query {
  chatRoomList: [ChatRoom!]!
}

type Mutation {
  createChatRoom(input: CreateChatRoomInput): ChatRoom!
}

type Subscription {
  postCreated: String
}

schema {
  query: Query
  mutation: Mutation
  subscription: Subscription
}
